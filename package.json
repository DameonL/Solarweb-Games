{
  "name": "solarweb",
  "version": "1.0.0",
  "scripts": {
    "build": "cd front && npm run build",
    "deploy": "npm run build",
    "dev": "cd front && npm run dev"
  },
  "dependencies": {
    "node": "^18.16.1"
  }
}
